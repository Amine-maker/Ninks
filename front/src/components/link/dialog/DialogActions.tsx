import { type DialogAction } from "@/hooks/DialogHook";
import React from "react";

type Props = DialogAction & {
  //
};

const DialogActions: React.FC<Props> = (props) => {
  return <div>DialogActions</div>;
};

export default DialogActions;
